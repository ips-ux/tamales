{
  "name": "bangin-bustos-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth,hosting",
    "deploy": "npm run build && firebase deploy --only functions",
    "seed": "node scripts/seed-firestore.mjs",
    "write-web-env": "node scripts/write-web-env.mjs",
    "set-admin": "node scripts/set-admin-claim.mjs"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
